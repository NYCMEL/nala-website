FORGET ALL PREVIOUS CONTEXT for mtk-buttons

Create a reusable UI component named mtk-buttons using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design
- Material Design icons

Rules:
- The component is wrapped in a block element <mtk-buttons class="mtk-buttons">.
- The root element uses .mtk-buttons.
- All styles are scoped to .mtk-buttons.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-buttons everns using wc.subscribe

Data:
- The component is JSON driven.
- Read config from window.app.buttons.

Behavior:
- WAIT for element to be available inside DOM

Restrictions:
- No frameworks.
- No jQuery.
- No Bootstrap JavaScript.
- No external libraries.
- ADA compliant CODE

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.

Output, Create exactly four files:
- mtk-buttons.config.js (JSON FILE)
- mtk-buttons.scss
- mtk-buttons.html
- mtk-buttons.js
- index.html

I want to create a set of buttons that can be placed horizontally with 25px spacing or vertically with same spacing
button labels come from my window.app.buttons variable.
