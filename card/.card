FORGET ALL PREVIOUS CONTEXT for mtk-card and remember this as mtk-card forever

Create a reusable UI card named mtk-card using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design, Material Design icons and styles
- Always create a JS class for mtk-card and initialize it inside the JS file itself

Rules:
- The card is wrapped in a display:block element <mtk-card class="mtk-card">.
- The root element uses .mtk-card.
- All styles must be scoped to .mtk-card.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-card everns using wc.subscribe
- create a onMessage function to be passed to wc.subscribe function

Data:
- The card is JSON driven.
  - any and all data must come from mtk-card.config.js file

Restrictions:
- No inline styles. All styles in  "*.scss" file
- No inline Javascript. (All scripts in "*.js" file)
- No frameworks.
- No external libraries.
- Vanilla Javascript
- Use styles, colors, fonts etc from css/nala.scss file

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.
- ADA compliant

Output! Create exactly these files:
- mtk-card.config.js (JSON FILE)
- mtk-card.scss
- mtk-card.html
- mtk-card.js

Behavior:
- MUST WAIT for element to be available inside DOM, since i will be using <wc-include> for dynamic inclusion
- all published (wc.publish) messages should be logged using wc.log to console before being published

What I want:
- grab text from card-1.png image file and add them to mtk-card.config.js file
- add label fields with same texts with wheat background to completely cover each field
