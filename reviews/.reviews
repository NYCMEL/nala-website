FORGET ALL PREVIOUS CONTEXT for mtk-reviews and remember this as mtk-reviews forever

Create a reusable UI reviews named mtk-reviews using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design, Material Design icons and styles
- Always create a JS class for mtk-reviews and initialize it inside the JS file itself

Rules:
- The reviews is wrapped in a display:block element <mtk-reviews class="mtk-reviews">.
- The root element uses .mtk-reviews.
- All styles must be scoped to .mtk-reviews.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-reviews everns using wc.subscribe
- create a onMessage function to be passed to wc.subscribe function

Data:
- The reviews is JSON driven.
  - any and all data must come from mtk-reviews.config.js file

Restrictions:
- No inline styles. (All styles in  "*.scss" file)
- No inline Javascript. (All scripts in "*.js" file)
- No frameworks.
- No external libraries.
- Vanilla Javascript

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.
- ADA compliant

Output! Create exactly these files:
- mtk-reviews.config.js (JSON FILE)
- mtk-reviews.scss
- minimal mtk-reviews.html
- mtk-reviews.js

Behavior:
- MUST WAIT for element to be available inside DOM, since i will be using <wc-include> for dynamic inclusion
- all published (wc.publish) messages should be logged using wc.log to console before being published

What I want:
- create a material design form from the image. allow user to pick star rating
