FORGET ALL PREVIOUS CONTEXT for mtk-investor and remember this as mtk-investor forever

Create a UI investor named mtk-investor using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design, Material Design icons and styles
- Always create a JS class for mtk-investor and initialize it inside the JS file itself

Rules:
- The investor is wrapped in a display:block element <mtk-investor class="mtk-investor">.
- The root element uses .mtk-investor.
- All styles must be scoped to .mtk-investor.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-investor everns using wc.subscribe
- create a onMessage function to be passed to wc.subscribe function

Data:
- The investor is JSON driven.
  - any and all data must come from mtk-investor.config.js file

Restrictions:
- No inline styles. All styles in  "*.scss" file
- No inline Javascript. (All scripts in "*.js" file)
- No frameworks.
- No external libraries.
- Vanilla Javascript
- Use styles, colors, fonts etc from css/nala.scss file

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.
- ADA compliant

Output! Create exactly these files:
- mtk-investor.config.js (JSON FILE)
- mtk-investor.scss
- mtk-investor.html
- mtk-investor.js

Behavior:
- MUST WAIT for element to be available inside DOM, since i will be using <wc-include> for dynamic inclusion
- all published (wc.publish) messages should be logged using wc.log to console before being published

What I want:
- a one page NALA investor Landing page

