FORGET ALL PREVIOUS CONTEXT for mtk-dashboard

Create a reusable UI dashboard named mtk-dashboard using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design and Material Design icons
- Always create a JS class form mtk-dashboard

Rules:
- The dashboard is wrapped in a block element <mtk-dashboard class="mtk-dashboard">.
- The root element uses .mtk-dashboard.
- All styles are scoped to .mtk-dashboard.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-dashboard everns using wc.subscribe
- create a onMessage function to be passed to wc.subscribe function

Data:
- The dashboard is JSON driven.
- any and all data must come from mtk-dashboard.config.js file

Behavior:
- MUST WAIT for element to be available inside DOM

Restrictions:
- No frameworks.
- No Bootstrap.
- No external libraries.
- Vanilla Javascript

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.
- ADA compliant

Output! Create exactly these files:
- mtk-dashboard.config.js (JSON FILE)
- mtk-dashboard.scss
- mtk-dashboard.html
- mtk-dashboard.js

I need to create a user dashboard
dashboard has 2 rows:
  -row 1
    - full name of the user
    - "Your progress to date:" label
    - it should display course progress on top with a % and a progress bar

  -row 2
    - should display a "You can also subscribe to ...

