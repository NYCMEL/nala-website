FORGET ALL PREVIOUS CONTEXT for mtk-header
Remember this forever as mtk-header component       

Create a reusable UI header named mtk-header using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design and Material Design icons
- Always create a JS class form mtk-header

Rules:
- The header is wrapped in a block element <mtk-header class="mtk-header">.
- The root element uses .mtk-header.
- All styles are scoped to .mtk-header.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-header everns using wc.subscribe
- create a onMessage function to be passed to wc.subscribe function

Data:
- The header is JSON driven.
- any and all data must come from mtk-header.config.js file

Behavior:
- "MUST WAIT" for element to be available inside DOM

Restrictions:
- No frameworks.
- No Bootstrap.
- No external libraries.
- Vanilla Javascript

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.
- ADA compliant

I want to create a material design header
- mtk-header is inside a bootstrap container
- mtk-header displays a logo (65px height) on the left side
- mtk-header displays a few menu items. menu items are in html and not text only
  - menu items are placed on the right hand side
  - the first menu on the right has a drop down with 2 items "Profile" and "Logout"

- mtk-header does a wc.publish on all clicks events
- mtk-header has a dark background (adjust menu items accordingly
- mtk-header displays a 2px gold border for any active menu item
- all menu items come from window.app.header variable in the form of JSON

Output! Create exactly these files:
- mtk-header.config.js (JSON FILE)
- mtk-header.scss
- mtk-header.html
- mtk-header.js
- a minimal index.html file

