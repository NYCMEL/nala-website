FORGET ALL PREVIOUS CONTEXT for mtk-settings and remember this as mtk-settings forever

Create a reusable UI settings named mtk-settings using:
- HTML
- SCSS
- Vanilla JavaScript
- Bootstrap 5 (layout only)
- Modern Material Design, Material Design icons and styles
- Always create a JS class for mtk-settings and initialize it inside the JS file itself

Rules:
- The settings is wrapped in a display:block element <mtk-settings class="mtk-settings">.
- The root element uses .mtk-settings.
- All styles must be scoped to .mtk-settings.
- No global styles or IDs.
- publish click events using wc.publish
- subscribe to all 4-mtk-settings everns using wc.subscribe
- create a onMessage function to be passed to wc.subscribe function

Data:
- The settings is JSON driven.
  - any and all data must come from mtk-settings.config.js file

Restrictions:
- No inline styles. All styles in  "*.scss" file
- No inline Javascript. (All scripts in "*.js" file)
- No frameworks.
- No external libraries.
- Vanilla Javascript
- Use styles, colors, fonts etc from css/nala.scss file

Accessibility:
- Use semantic HTML.
- Keyboard accessible.
- Visible focus states.
- ADA compliant

Output! Create exactly these files:
- mtk-settings.config.js (JSON FILE)
- mtk-settings.scss
- mtk-settings.html
- mtk-settings.js

Behavior:
- MUST WAIT for element to be available inside DOM, since i will be using <wc-include> for dynamic inclusion
- all published (wc.publish) messages should be logged using wc.log to console before being published

What I want:
- I want to create a new page for "Profile & Settings":
- 3 tabs: Privacy, Business Information, Services Offered
